// ============================================================
//  steps/inputmap/mod.rs — 入力アクションマップ（`.inputmap`）の変換段
//
//  段を足すときは `vN_to_vM.rs` を作ってここに `pub mod` を足し、
//  `registry.rs` の表へ 1 行、`kind.rs` の現行版を +1 する。
// ============================================================

/// v1（`bindings` にすべてのバインドが入る形）→ v2（軸ごとの正負グループ）。
pub mod v1_to_v2;
