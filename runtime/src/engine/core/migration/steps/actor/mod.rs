// ============================================================
//  steps/actor/mod.rs — アクタ（`.actor` / `.actor2d`）の変換段
//
//  段を足すときは `vN_to_vM.rs` を作ってここに `pub mod` を足し、
//  `registry.rs` の表へ 1 行、`kind.rs` の現行版を +1 する。
// ============================================================

/// v1（欄なしの従来形式）→ v2（旧 enum 表記の正規化）。
pub mod v1_to_v2;
